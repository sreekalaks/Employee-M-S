               Employee Management system

1 create a project - npx create-react-app-ems
2 To run a project -npm start
3 Bootstrap - npm i react-bootstrap bootstrap
4 bootswatch -For Theme
5 creating components
6 Employee.js - create a array of employee data
7 Home.js -To create a home page
8 Icons - react icons github icons
                    npm i react-Icons
9 Routing - To render one page to another
                    npm i react-router-dom
                    import {BrowserRouter , Routes , Route}
          Routes-Declares an element that should be rendered at a certain URL path.
          Routes-A container for a nested tree of elements that renders the branch that best matches the current location.
          In simple terms it is a collection of router
          BrowserRouter -A <Router> for use in web browsers. Provides the cleanest URL
10 Add.js - To add Employee
11 Edit.js - To edit existing Employee
12.Routing - To render one page to another
                npm i react-router-dom
                import {BrowserRouter , Routes , Route}
        Route - Declares an element that should be rendered at a certain URL path.
        Routes - A container for a nested tree of elements that renders the branch that best matches the current location.
                In simple terms It's a collection of route.
        BrowserRouter - A <Router> for use in web browsers. Provides the cleanest URLs.
     13. Link import {Link} from react-router-dom
                   <Link to={'/add'}></Link>
     14. Delete Button
     15. useNavigate() - Returns an imperative method for changing the location. Used by s, but may also be used by other elements to change the location.